# M8b P3 A3 穿透反馈 + A4 徽标/全屏 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans。步骤用 `- [ ]`。

**Goal:** A3 穿透/可交互模式切换的瞬间反馈（涟漪 + toast）；A4 DND/专注/隐藏状态徽标 + 全屏检测自动隐藏。

**Architecture:** A3 复用 `display.clickThrough` pref 作模式真源（翻转即切换）+ character 窗 DOM 反馈层。A4 状态机纯逻辑（normal/dnd/focus/hidden）+ character 徽标 DOM；新 pref `display.dndManual`/`display.focusMode`；全屏检测为 Main best-effort 轮询（**需真机校准**，附 fallback）。

**关联 spec:** [`../spec.md`](../spec.md)（§1 A3/A4）。**前置：M8b P1/P2 已落**。

> ⚠️ **全屏检测**：Electron 无跨平台"前台应用是否全屏"标准 API。本期 Win 用 best-effort 启发式（前台窗矩形≈屏幕工作区→视为全屏），**真机验证后调阈**；检测不可靠时 fallback = 仅手动隐藏（托盘/热键，M8c）。此点在 RESULTS 标残留。

---

## 文件结构
- 改 `packages/protocol/src/prefs.ts`（+`display.dndManual`、`display.focusMode`）、`methods.ts`（+`app.desktopState` 通知 + `app.window.toggleClickThrough`）
- 新 `apps/desktop/src/renderer/character/desktop-state.ts`（纯：mode 状态机）
- 新 `apps/desktop/electron/main/fullscreen-watch.ts`（best-effort 轮询，注入便于测）
- 改 `character/index.html`（+`#fx`/`#badge` 层）、`character/main.ts`（A3 反馈 + A4 徽标）、`ipc-router.ts`/`index.ts`（toggleClickThrough 真源 + desktopState 广播 + 全屏轮询）
- 测试：`desktop-state.test.ts`、`fullscreen-watch.test.ts`、`prefs.test.ts`(追加)

---

## Task 1: prefs + desktop mode 状态机（纯）

**Files:** Modify `packages/protocol/src/prefs.ts`；Create `apps/desktop/src/renderer/character/desktop-state.ts`；Test 两个

- [ ] **Step 1: prefs 失败测试 + 状态机失败测试**

```ts
// packages/protocol/test/prefs.test.ts 追加
it('dndManual/focusMode 默认 false', () => {
  expect(DEFAULT_PREFS['display.dndManual']).toBe(false);
  expect(DEFAULT_PREFS['display.focusMode']).toBe(false);
});
```
```ts
// apps/desktop/test/character/desktop-state.test.ts
import { describe, it, expect } from 'vitest';
import { resolveMode } from '../../src/renderer/character/desktop-state';

describe('desktop-state（A4 模式优先级）', () => {
  it('优先级 hidden > focus > dnd > normal', () => {
    expect(resolveMode({ fullscreenHidden: true, focus: true, dnd: true })).toBe('hidden');
    expect(resolveMode({ fullscreenHidden: false, focus: true, dnd: true })).toBe('focus');
    expect(resolveMode({ fullscreenHidden: false, focus: false, dnd: true })).toBe('dnd');
    expect(resolveMode({ fullscreenHidden: false, focus: false, dnd: false })).toBe('normal');
  });
});
```

- [ ] **Step 2: 失败** — 两测 FAIL。

- [ ] **Step 3: 实现**

prefs（`display.*` 段）：
```ts
  'display.dndManual': z.boolean().default(false),
  'display.focusMode': z.boolean().default(false),
```
```ts
// apps/desktop/src/renderer/character/desktop-state.ts
/** A4 角色存在感模式（纯函数）。优先级：隐藏 > 专注 > 勿扰 > 正常。 */
export type DesktopMode = 'normal' | 'dnd' | 'focus' | 'hidden';
export function resolveMode(s: { fullscreenHidden: boolean; focus: boolean; dnd: boolean }): DesktopMode {
  if (s.fullscreenHidden) return 'hidden';
  if (s.focus) return 'focus';
  if (s.dnd) return 'dnd';
  return 'normal';
}
```

- [ ] **Step 4: 通过 + 重建 protocol** — 两测 PASS；`pnpm --filter @desksoul/protocol build`。

- [ ] **Step 5: 提交**

```bash
git add packages/protocol/src/prefs.ts apps/desktop/src/renderer/character/desktop-state.ts apps/desktop/test/character/desktop-state.test.ts packages/protocol/test/prefs.test.ts
git commit -m "feat: A4 dnd/focus prefs + desktop mode state machine (pure)"
```

---

## Task 2: 全屏检测（Main best-effort，注入可测）

**Files:** Modify `packages/protocol/src/methods.ts`；Create `apps/desktop/electron/main/fullscreen-watch.ts`；Test `apps/desktop/test/fullscreen-watch.test.ts`

- [ ] **Step 1: 写失败测试（纯判定 + 轮询回调）**

```ts
// apps/desktop/test/fullscreen-watch.test.ts
import { describe, it, expect, vi } from 'vitest';
import { isLikelyFullscreen, createFullscreenWatch } from '../electron/main/fullscreen-watch';

describe('fullscreen-watch（best-effort）', () => {
  it('isLikelyFullscreen：前台窗矩形≈工作区视为全屏', () => {
    expect(isLikelyFullscreen({ x: 0, y: 0, w: 1920, h: 1080 }, { w: 1920, h: 1080 })).toBe(true);
    expect(isLikelyFullscreen({ x: 100, y: 100, w: 800, h: 600 }, { w: 1920, h: 1080 })).toBe(false);
  });
  it('createFullscreenWatch：状态变化才回调', () => {
    const states = [false, false, true, true, false];
    let i = 0;
    const onChange = vi.fn();
    const w = createFullscreenWatch({ probe: () => states[i++] ?? false, onChange });
    for (let k = 0; k < states.length; k++) w.tick();
    expect(onChange.mock.calls.map((c) => c[0])).toEqual([true, false]); // 仅变化沿
    w.stop();
  });
});
```

- [ ] **Step 2: 失败** — FAIL。

- [ ] **Step 3: 实现**

methods.ts（通知区）加：
```ts
  'app.desktopState': {
    params: z.object({ fullscreen: z.boolean() }),
    result: z.null(),
  },
```
```ts
// apps/desktop/electron/main/fullscreen-watch.ts
/**
 * 全屏检测（best-effort）。Electron 无跨平台"前台应用全屏"API：
 * probe() 由 index.ts 注入（Win 可用前台窗矩形≈屏幕；不可靠时 probe 恒 false，
 * 退化为仅手动隐藏）。**真机校准后调 isLikelyFullscreen 阈值**。
 */
export interface Rect { x: number; y: number; w: number; h: number; }
export function isLikelyFullscreen(win: Rect, screen: { w: number; h: number }): boolean {
  return win.x <= 0 && win.y <= 0 && win.w >= screen.w && win.h >= screen.h;
}

export interface FullscreenWatch { tick(): void; stop(): void; }
export function createFullscreenWatch(deps: {
  probe: () => boolean;
  onChange: (fullscreen: boolean) => void;
  intervalMs?: number;
}): FullscreenWatch {
  let last: boolean | null = null;
  let timer: ReturnType<typeof setInterval> | null = null;
  const tick = (): void => {
    const v = deps.probe();
    if (v !== last) {
      last = v;
      deps.onChange(v);
    }
  };
  if (deps.intervalMs) timer = setInterval(tick, deps.intervalMs);
  return { tick, stop: () => { if (timer) clearInterval(timer); } };
}
```

- [ ] **Step 4: 通过 + 重建** — PASS (2)；`pnpm --filter @desksoul/protocol build`。

- [ ] **Step 5: 提交**

```bash
git add packages/protocol/src/methods.ts apps/desktop/electron/main/fullscreen-watch.ts apps/desktop/test/fullscreen-watch.test.ts
git commit -m "feat(desktop): best-effort fullscreen watch + app.desktopState notify"
```

---

## Task 3: A3 穿透反馈 + toggleClickThrough 真源

**Files:** Modify `packages/protocol/src/methods.ts`、`ipc-router.ts`、`character/index.html`、`character/main.ts`

- [ ] **Step 1: methods 加 `app.window.toggleClickThrough`**

```ts
  'app.window.toggleClickThrough': {
    params: z.object({}),
    result: z.object({ ok: z.literal(true), ignore: z.boolean() }),
  },
```

- [ ] **Step 2: ipc-router 实现（翻转 display.clickThrough pref + 施加 + 返回新态）**

`createRouter` 内加：
```ts
    'app.window.toggleClickThrough': () => {
      const cur = prefsStore.getAll()['display.clickThrough'];
      const next = !cur;
      prefsStore.set('display.clickThrough', next);
      const c = deps.characterWindow();
      if (c && !c.isDestroyed()) c.setIgnoreMouseEvents(next, { forward: true });
      broadcast('app.prefs.changed', { key: 'display.clickThrough', value: next });
      return { ok: true as const, ignore: next };
    },
```
并把 P1 Task3 的 `toggleClickThrough` 占位改为调用此真源逻辑（菜单项里 `prefsStore.set` + 同上）。

- [ ] **Step 3: character A3 反馈层**

index.html 加 `<div id="fx"></div>` + 涟漪/toast 的 CSS（涟漪 = 扩散圆，toast = 顶部条 500ms 淡出）。main.ts 订阅 `app.prefs.changed`（key==='display.clickThrough'）→ 播涟漪（穿透=青 `--ds-cool` / 恢复=暖）+ toast 文案（`🔇 鼠标穿透已开启` / `✋ 已恢复互动`）。

> 常态脚下色条（A3 可选，默认关）：D4 `display.clickThroughBar` 已持久；character 读之渲染脚下 4px 条（暖/青）。本步可一并接（pref 已存）。

- [ ] **Step 4: typecheck + 提交**

```bash
pnpm --filter @desksoul/desktop typecheck
pnpm exec prettier --write apps/desktop/electron/main/ipc-router.ts apps/desktop/src/renderer/character/main.ts apps/desktop/src/renderer/character/index.html packages/protocol/src/methods.ts
git add -A
git commit -m "feat: A3 click-through toggle truth-source + ripple/toast feedback"
```

---

## Task 4: A4 徽标 + 接全屏轮询

**Files:** Modify `character/index.html`（`#badge`）、`character/main.ts`（徽标渲染 by resolveMode）、`index.ts`（启动 fullscreen-watch + broadcast app.desktopState + probe 注入）

- [ ] **Step 1: index.ts 启动全屏轮询**

`whenReady` 内加（probe best-effort；Win 取前台窗矩形对比主屏，无可靠源时恒 false）：
```ts
  const fsWatch = createFullscreenWatch({
    probe: () => false, // TODO(真机)：接 Win 前台窗矩形检测；不可靠则保持 false（仅手动隐藏）
    onChange: (fullscreen) => {
      for (const wc of rendererTargets(wins!)()) if (!wc.isDestroyed()) wc.send('desksoul:notify:app.desktopState', { fullscreen });
    },
    intervalMs: 1500,
  });
```
`before-quit` 加 `fsWatch.stop()`。import `createFullscreenWatch`。

- [ ] **Step 2: character main.ts A4 徽标**

订阅 `app.desktopState`（fullscreen）+ `app.prefs.changed`（dndManual/focusMode）→ 维护三标志 → `resolveMode` → 应用：
- `hidden` → 隐藏角色（`#stage`/`#fallback` opacity 0 或请求 Main 隐藏窗）
- `focus` → 半透明 30%
- `dnd` → 显示肩部月牙徽标（`#badge` 显示 🌙）+ 气泡降级（A2 bubble 改为头顶脉冲，不展开）
- `normal` → 清除

index.html 加 `#badge` 元素 + 样式（小徽标，绝对定位近肩部）。

- [ ] **Step 3: typecheck + 全量 + 提交**

```bash
pnpm --filter @desksoul/desktop typecheck
pnpm --filter @desksoul/desktop test
pnpm exec prettier --write apps/desktop/electron/main/index.ts apps/desktop/src/renderer/character/
git add -A
git commit -m "feat: A4 dnd/focus/hidden badges + fullscreen-driven state"
```

---

## Self-Review（plan vs spec A3/A4）
- **A3 切换反馈 + 真源**：T1 toggleClickThrough（pref 真源）+ T3 涟漪/toast + 常态色条（pref 已存）✓。
- **A4 状态机 + 徽标 + 全屏**：T1 resolveMode + T2 全屏 watch + T4 徽标渲染 ✓。
- **DND 气泡降级**：T4（dnd → A2 头顶脉冲不展开）衔接 P2 ✓。
- **占位符/诚实**：全屏 probe 明标 TODO(真机) + fallback（恒 false=仅手动隐藏）；RESULTS 记残留。非空泛占位——给了可跑的退化路径。
- **类型一致**：`resolveMode`(T1) ↔ main(T4)；`isLikelyFullscreen/createFullscreenWatch`(T2) ↔ index(T4 Step1)；`app.desktopState`/`toggleClickThrough` schema ↔ 用法一致。
- **回归点**：toggleClickThrough 写 pref 真源（与 M7b-1 clickThrough effect 一致路径）；全屏 probe 默认 false → 不引入误隐藏。
