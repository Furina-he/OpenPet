# M7b-1 P2 实施计划 · D4 显示与窗口

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans 逐 task 实现。步骤用 `- [ ]`。

**Goal:** 把 `system.display` 从 M7a 的"只有界面主题"扩成完整 D4 面板（ui-design §7.4），落地复用件 `SettingSection`，并完成 `display.characterScale` 收编（slider→prefs→effect 设窗口尺寸 + 同步 `characterSize` 真源）。

**Architecture:** 沿用 M7a 面板范式（onMounted `app.prefs.getAll` 回填 → 控件 change → `app.prefs.set` → 顶栏 `✓ 已保存`）。窗口几何副作用走 P1 的 effects 表：本阶段给 `display.characterScale` 加 effect（`createPrefEffects` 内，注入 `setCharacterSize` 回写 ipc-router 真源）。`alwaysOnTop/clickThrough` effect 已在 P1；`theme` 走广播已在 M7a。

**Tech Stack:** Vue 3 SFC、Tailwind token、Electron BrowserWindow、`window-scale.scaledBounds`、Vitest（纯逻辑下沉，不引入 @vue/test-utils）。

**关联：** spec `docs/plans/2026-06-17-m7b1-d-series-spec.md`；前置 P1 已落（254/177 绿）。分支 `feat/m7b1-d-series`。

**范围界定（重要）：**
- IN：D4 全部开关**渲染 + 持久化**；`characterScale/alwaysOnTop/clickThrough/theme` **即时生效**。
- 持久但本阶段不接 live 行为（存而不接，§7 全量持久策略）：`lookAtStrength/physics/clickThroughBar/wallpaperMode/followDisplay/crossScreenDrag/fullscreenHide/gameDetect/meetingDowngrade`，以及 `lookAt/footGlow` 的 **character 渲染端 live 响应**（其 Main 广播已发，渲染端消费留到 M8 角色交互回顾；本阶段二者先持久化）。穿透热键展示为静态占位（J2 录制器在 M8）。

---

## 文件结构
- 新 `apps/desktop/src/renderer/components/SettingSection.vue`
- 改 `apps/desktop/electron/main/prefs/effects.ts`（加 `display.characterScale` effect + `setCharacterSize` dep）
- 改 `apps/desktop/electron/main/ipc-router.ts`（`characterSize` 声明上移到 effects 构造前；传 `setCharacterSize`）
- 改 `apps/desktop/src/renderer/settings/pages/DisplayPage.vue`（扩成完整 D4）
- 测试：改 `apps/desktop/test/prefs/effects.test.ts`（+characterScale 用例）
- 收尾：追加 `apps/desktop/RESULTS-M7b1.md`（P2 段）

---

## Task 1: SettingSection 组件（带标题分组卡）

**Files:** Create `apps/desktop/src/renderer/components/SettingSection.vue`

> §7.1：同主题项目放一组卡片（圆角 12，内分隔线 `rgba(white,.06)`）；危险/实验区可传 `tone="warn"` 走警告色描边（§7.4 实验性）。纯展示，无单测，靠 typecheck + 集成。

- [ ] **Step 1: 写组件**

```vue
<!-- apps/desktop/src/renderer/components/SettingSection.vue -->
<script setup lang="ts">
defineProps<{ title: string; tone?: 'default' | 'warn' }>();
</script>
<template>
  <section
    class="mb-4 overflow-hidden rounded-card border bg-glass-bg backdrop-blur-glass"
    :class="tone === 'warn' ? 'border-warning' : 'border-glass-border'"
  >
    <header class="px-4 pt-3 text-sm text-text-sub">{{ title }}</header>
    <!-- 行间分隔线：子项用 SettingCard，靠 divide 工具类描边 -->
    <div class="divide-y" style="--tw-divide-opacity: 1; border-color: rgba(127, 127, 127, 0.12)">
      <slot />
    </div>
  </section>
</template>
```

- [ ] **Step 2: typecheck**

Run: `pnpm --filter @desksoul/desktop typecheck`
Expected: 干净。

- [ ] **Step 3: 提交**

```bash
git add apps/desktop/src/renderer/components/SettingSection.vue
git commit -m "feat(desktop): SettingSection grouped-card component (§7.1; warn tone for §7.4 experimental)"
```

---

## Task 2: characterScale effect（收编 setScale + 同步 characterSize 真源）

**Files:** Modify `apps/desktop/electron/main/prefs/effects.ts`、`apps/desktop/electron/main/ipc-router.ts`；Modify `apps/desktop/test/prefs/effects.test.ts`

- [ ] **Step 1: 追加失败测试（characterScale effect 设 bounds + 回写尺寸）**

```ts
// apps/desktop/test/prefs/effects.test.ts — 在 describe 内追加
it('characterScale → setBounds(scaledBounds) + setCharacterSize', () => {
  const setBounds = vi.fn();
  const setCharacterSize = vi.fn();
  const win = {
    isDestroyed: () => false,
    setAlwaysOnTop: vi.fn(),
    setIgnoreMouseEvents: vi.fn(),
    getBounds: () => ({ x: 100, y: 100, width: 320, height: 480 }),
    setBounds,
  };
  const effects = createPrefEffects({ characterWindow: () => win as never, setCharacterSize });
  effects['display.characterScale']!(0.5);
  // base 320×480 @0.5 → 160×240，底边中点锚定
  expect(setBounds).toHaveBeenCalledWith({ x: 180, y: 340, width: 160, height: 240 });
  expect(setCharacterSize).toHaveBeenCalledWith({ width: 160, height: 240 });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm --filter @desksoul/desktop exec vitest run test/prefs/effects.test.ts`
Expected: FAIL — `effects['display.characterScale']` undefined。

- [ ] **Step 3a: 实现 effects.ts**

import 区加：
```ts
import { scaledBounds } from '../window-scale.js';
```
`EffectsDeps` 加字段：
```ts
  /** 缩放后回写 ipc-router 的 characterSize 真源（moveBy 锁尺寸用）。 */
  setCharacterSize?: (size: { width: number; height: number }) => void;
```
在 `createPrefEffects` 返回对象里追加（与 alwaysOnTop 等并列）：
```ts
    'display.characterScale': (v) => {
      const w = win();
      if (!w) return;
      const b = scaledBounds(w.getBounds(), v);
      deps.setCharacterSize?.({ width: b.width, height: b.height });
      w.setBounds(b);
    },
```

- [ ] **Step 3b: 实现 ipc-router.ts（characterSize 上移 + 传 setCharacterSize）**

把 `let characterSize = { ...CHARACTER_BASE_SIZE };`（及其上方注释）**移到** `const prefEffects = ...` 构造**之前**。
把 prefEffects 构造改为（在 P1 基础上加 `setCharacterSize`）：
```ts
  const prefEffects =
    deps.prefEffects ??
    createPrefEffects({
      characterWindow: deps.characterWindow,
      setLoginItem: deps.setLoginItem ?? (() => {}),
      setCharacterSize: (s) => {
        characterSize = s;
      },
      broadcast,
    });
```
（`character.setScale` RPC、`moveBy` 不变——前者留作拖动实时预览、后者继续用 `characterSize` 锁尺寸。启动 `applyAllEffects` 现会按 `display.characterScale` 还原窗口尺寸 + characterSize。）

- [ ] **Step 4: 跑测试 + typecheck**

Run:
```bash
pnpm --filter @desksoul/desktop exec vitest run test/prefs/effects.test.ts
pnpm --filter @desksoul/desktop typecheck
```
Expected: effects 测全过（含新用例）；typecheck 干净。

- [ ] **Step 5: 提交**

```bash
git add apps/desktop/electron/main/prefs/effects.ts apps/desktop/electron/main/ipc-router.ts apps/desktop/test/prefs/effects.test.ts
git commit -m "feat(desktop): display.characterScale effect (setBounds + sync characterSize source)"
```

---

## Task 3: DisplayPage 扩成完整 D4

**Files:** Modify `apps/desktop/src/renderer/settings/pages/DisplayPage.vue`

> 整体替换。沿用 onMounted getAll + 控件 change→set→saved。缩放 slider：`@input` 走 `character.setScale`（实时预览不落盘）、`@change` 走 `app.prefs.set`（持久 + effect + toast）。所有 D4 键渲染并绑定（含存而不接项）。

- [ ] **Step 1: 整体替换 DisplayPage.vue**

```vue
<!-- apps/desktop/src/renderer/settings/pages/DisplayPage.vue — D4 显示与窗口（ui-design §7.4） -->
<script setup lang="ts">
import { onMounted, ref } from 'vue';
import type { Prefs, PrefKey } from '@desksoul/protocol';
import { DEFAULT_PREFS } from '@desksoul/protocol';
import SettingSection from '../../components/SettingSection.vue';
import SettingCard from '../../components/SettingCard.vue';
import Switch from '../../components/Switch.vue';
import Select from '../../components/Select.vue';
import Slider from '../../components/Slider.vue';

const emit = defineEmits<{ saved: [] }>();
const prefs = ref<Prefs>({ ...DEFAULT_PREFS });

onMounted(async () => {
  prefs.value = (await window.desksoul.rpc('app.prefs.getAll', {})) as Prefs;
});

// 通用：写一个 pref → 乐观更新 + 持久 + 顶栏 toast。
async function set<K extends PrefKey>(key: K, value: Prefs[K]): Promise<void> {
  prefs.value = { ...prefs.value, [key]: value };
  await window.desksoul.rpc('app.prefs.set', { key, value });
  emit('saved');
}
// 缩放：拖动实时预览（不落盘），松手持久。
function previewScale(v: number): void {
  prefs.value = { ...prefs.value, 'display.characterScale': v };
  void window.desksoul.rpc('character.setScale', { scale: v });
}
function commitScale(v: number): void {
  void set('display.characterScale', v);
}

const THEME = [
  { value: 'system', label: '跟随系统' },
  { value: 'light', label: '浅色' },
  { value: 'dark', label: '深色' },
];
const DISPLAYS = [{ value: 'primary', label: '主显示器' }];
const DRAG = [
  { value: 'snap', label: '吸附到目标屏边缘' },
  { value: 'free', label: '自由跨屏' },
];
</script>

<template>
  <div class="max-w-[640px]">
    <SettingSection title="角色">
      <SettingCard label="缩放" :description="`${Math.round(prefs['display.characterScale'] * 100)}%`">
        <Slider
          :model-value="prefs['display.characterScale']"
          :min="0.5"
          :max="2"
          :step="0.05"
          @update:model-value="previewScale"
          @change="commitScale(prefs['display.characterScale'])"
        />
      </SettingCard>
      <SettingCard label="始终置顶">
        <Switch
          :model-value="prefs['display.alwaysOnTop']"
          @update:model-value="(v) => set('display.alwaysOnTop', v)"
        />
      </SettingCard>
      <SettingCard label="鼠标穿透">
        <Switch
          :model-value="prefs['display.clickThrough']"
          @update:model-value="(v) => set('display.clickThrough', v)"
        />
      </SettingCard>
      <SettingCard label="穿透模式常态色条" description="默认关">
        <Switch
          :model-value="prefs['display.clickThroughBar']"
          @update:model-value="(v) => set('display.clickThroughBar', v)"
        />
      </SettingCard>
      <SettingCard label="LookAt 看向鼠标">
        <Switch
          :model-value="prefs['display.lookAt']"
          @update:model-value="(v) => set('display.lookAt', v)"
        />
      </SettingCard>
      <SettingCard label="LookAt 强度">
        <Slider
          :model-value="prefs['display.lookAtStrength']"
          :min="0"
          :max="100"
          @update:model-value="(v) => set('display.lookAtStrength', v)"
        />
      </SettingCard>
      <SettingCard label="物理摆动（头发/裙摆）">
        <Switch
          :model-value="prefs['display.physics']"
          @update:model-value="(v) => set('display.physics', v)"
        />
      </SettingCard>
      <SettingCard label="角色脚下光晕" description="默认关">
        <Switch
          :model-value="prefs['display.footGlow']"
          @update:model-value="(v) => set('display.footGlow', v)"
        />
      </SettingCard>
      <SettingCard label="界面主题">
        <Select
          :model-value="prefs['display.theme']"
          :options="THEME"
          @update:model-value="(v) => set('display.theme', v as Prefs['display.theme'])"
        />
      </SettingCard>
    </SettingSection>

    <SettingSection title="多显示器">
      <SettingCard label="跟随显示器">
        <Select
          :model-value="prefs['display.followDisplay']"
          :options="DISPLAYS"
          @update:model-value="(v) => set('display.followDisplay', v)"
        />
      </SettingCard>
      <SettingCard label="跨屏拖动行为">
        <Select
          :model-value="prefs['display.crossScreenDrag']"
          :options="DRAG"
          @update:model-value="(v) => set('display.crossScreenDrag', v as Prefs['display.crossScreenDrag'])"
        />
      </SettingCard>
    </SettingSection>

    <SettingSection title="不打扰">
      <SettingCard label="全屏应用时自动隐藏">
        <Switch
          :model-value="prefs['display.fullscreenHide']"
          @update:model-value="(v) => set('display.fullscreenHide', v)"
        />
      </SettingCard>
      <SettingCard label="游戏检测">
        <Switch
          :model-value="prefs['display.gameDetect']"
          @update:model-value="(v) => set('display.gameDetect', v)"
        />
      </SettingCard>
      <SettingCard label="视频会议时降级为轮廓">
        <Switch
          :model-value="prefs['display.meetingDowngrade']"
          @update:model-value="(v) => set('display.meetingDowngrade', v)"
        />
      </SettingCard>
    </SettingSection>

    <SettingSection title="⚠ 实验性" tone="warn">
      <SettingCard
        label="桌面壁纸层模式（角色不抢 Z 序）"
        description="此模式下点击穿透行为可能与你的桌面工具冲突"
      >
        <Switch
          :model-value="prefs['display.wallpaperMode']"
          @update:model-value="(v) => set('display.wallpaperMode', v)"
        />
      </SettingCard>
    </SettingSection>
  </div>
</template>
```

- [ ] **Step 2: typecheck（重点：所有 set(key,value) 的键值类型对得上 PrefsSchema）**

Run: `pnpm --filter @desksoul/desktop typecheck`
Expected: 干净。若 `Select` 的 string→联合类型报错，按上面的 `as Prefs['...']` 收窄。

- [ ] **Step 3: 提交**

```bash
git add apps/desktop/src/renderer/settings/pages/DisplayPage.vue
git commit -m "feat(desktop): full D4 display panel (sections; scale live-preview+persist; all keys bound)"
```

---

## Task 4: 验收 + RESULTS 追加

**Files:** Modify `apps/desktop/RESULTS-M7b1.md`

- [ ] **Step 1: 全量回归 + typecheck + 格式（只格式化本阶段新写文件）**

Run:
```bash
pnpm --filter @desksoul/sidecar build
pnpm --filter @desksoul/desktop test
pnpm --filter @desksoul/desktop typecheck
pnpm exec prettier --write apps/desktop/src/renderer/components/SettingSection.vue apps/desktop/src/renderer/settings/pages/DisplayPage.vue apps/desktop/electron/main/prefs/effects.ts
```
Expected: desktop 全量绿（254 + effects 新用例）；typecheck 干净；prettier 仅动本阶段文件（**勿** `--write` ipc-router.ts 以免带出存量欠账，手验其新增行已合规）。

- [ ] **Step 2: 手动冒烟（D4 即时生效）**

`pnpm --filter @desksoul/desktop dev`：
- 缩放 slider 拖动 → 角色实时缩放；松手 → 顶栏 `✓ 已保存`；重启 app → 缩放保持。
- 始终置顶 / 鼠标穿透切换 → 角色窗即时响应。
- 界面主题切换 → 全窗换肤（M7a 行为不回归）。
- 其余开关可切换、重启保持（live 行为留后续）。

- [ ] **Step 3: 追加 RESULTS-M7b1.md 的 P2 段 + 提交**

记：D4 面板交付项、characterScale 收编方式（slider 双路径 + characterSize 同步）、存而不接清单（lookAt/footGlow live 渲染端响应 + 多显示器/不打扰/实验性留后续）、测试增量、手动冒烟结果。

```bash
git add apps/desktop/RESULTS-M7b1.md
git commit -m "docs(m7b1): RESULTS P2 (D4 display panel + characterScale收编)"
```

---

## Self-Review（plan vs spec）
- **D4 全量渲染 + 持久化**：T3 覆盖 §7.4 全部 section/开关，均绑定 prefs ✓。
- **有后端即时生效**：characterScale(T2 effect)/alwaysOnTop/clickThrough(P1)/theme(M7a) ✓；lookAt/footGlow 持久 + Main 广播已发，**渲染端 live 响应显式延后**（范围界定已声明）✓。
- **characterScale 收编**：effect 设 bounds + 同步 `characterSize`（T2），slider 双路径（T3）✓。
- **复用件**：SettingSection(T1) ✓；Switch/Select/Slider/SettingCard 复用 M7a ✓。
- **占位符**：无 TBD；代码步含完整代码。
- **类型一致**：`set<K extends PrefKey>(key,value)`(T3) 依赖 P1 已落的 `PrefKey`/`Prefs`；effect `setCharacterSize` dep(T2 effects.ts) ↔ ipc-router 传入(T2 ipc-router) 字段名一致 ✓。
- **风险**：`Slider` 当前是否暴露 `@change`（松手）事件？若仅 `@input`，commitScale 改为 slider 容器 `@pointerup` 或加去抖——实现时核 `Slider.vue`，必要时给它补 `change` 转发（原生 `<input type=range>` 有 change 事件，透传即可）。
