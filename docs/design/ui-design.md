# DeskSoul UI 设计规格

| 版本 | 日期 | 状态 | 关联文档 |
| --- | --- | --- | --- |
| v0.1 | 2026-05-01 | 初版（含 ASCII 线稿） | `PRD.md` v0.1 |
| v0.2 | 2026-06-19 | **重写：以高保真图为视觉真源，删除全部 ASCII 线稿** | `PRD.md` v0.1 |

> **本文定位（v0.2 起）**：UI 的**视觉真源 = `UI/` 目录下的高保真设计图 PNG**（19 张，覆盖全部 A–J 43 屏，100%）。本文档退为「**图索引 + 实现契约**」——只保留高保真图表达不了的东西：设计系统 token 精确值（§2）、信息架构（§3）、各屏的交互/行为/字段→prefs 契约、关键开关默认值（§14.1）。
>
> **做任何界面前**：先看该屏对应的 `UI/*.png`（映射见 §4），再读本文该屏小节的「交互/行为契约」。**不要凭文字想象视觉**——视觉一律以图为准；文字与图冲突时以图为准。
>
> **可交互参考 demo**（前端开发时在浏览器打开，参考交互/流程/状态/过渡；token 已对齐 §2）：`docs/demo/desksoul-product-interactive-demo.html`（产品全局：导航 / 主面板 / inspector / toast / 模态 / 托盘菜单 / 聊天窗 / 气泡 / 热键卡）+ `docs/demo/desksoul-settings-demo.html`（设置页早期 demo）。**分工：静态高保真图（`UI/`）= 视觉真源；demo = 交互/动态行为参考**，二者互补。
>
> 视觉风格：**毛玻璃 + 拟人混合（C 方案）**，**浅色默认 / 深色备选**（D4 可切换或跟随系统）。

---

## 目录

1. [术语表](#1-术语表)
2. [设计系统基础](#2-设计系统基础)
3. [信息架构与导航](#3-信息架构与导航)
4. [UI 设计图索引](#4-ui-设计图索引)
5. [A · 桌面层（A1–A4）](#5-a--桌面层a1a4)
6. [B · 聊天系统（B1–B5）](#6-b--聊天系统b1b5)
7. [C · 首次启动引导（C1–C4）](#7-c--首次启动引导c1c4)
8. [D · 设置面板（D1–D8）](#8-d--设置面板d1d8)
9. [E · 角色管理（E1–E5）](#9-e--角色管理e1e5)
10. [F · 记忆与人格（F1–F4）](#10-f--记忆与人格f1f4)
11. [G · 插件（G1–G4）](#11-g--插件g1g4)
12. [H · 知识库 & Agent（H1–H4）](#12-h--知识库--agenth1h4)
13. [I · IM 桥接（I1–I2）](#13-i--im-桥接i1i2)
14. [J · 系统集成（J1–J5）](#14-j--系统集成j1j5)
15. [附录](#15-附录)

---

## 1. 术语表

| 术语 | 定义 | 不要写成 |
| --- | --- | --- |
| **桌面气泡** | A2 那种贴在角色头顶的轻量台词浮窗 | 台词气泡 / A2 气泡 |
| **聊天浮层** | B1 完整对话窗口，可独立移动 | Chat overlay / 聊天窗口 |
| **Hub Window** | Layer 3 的统一管理窗口 | 设置主页 / 主面板 |
| **Provider** | LLM 服务方（OpenAI/Claude/Ollama 等） | 服务商 / 厂商 |
| **权限** | 插件/工具向用户申请的能力（网络/文件等） | 授权 / 许可 |
| **角色包** | `.dspack` 单位，含模型+动画+人格+元数据 | Character pack（中文文档内） |
| **演示模式** | 未配置 LLM 时角色用预设台词兜底的状态 | 离线模式（J4 才是离线） |
| **Layer 1/2/3** | 桌面层 / 浮层 / 窗口层（详见 §3） | — |

---

## 2. 设计系统基础

> §2 是**实现真源**：高保真图无法精确传达 backdrop-filter、色值、圆角等数值，一律以本节为准。

### 2.1 色彩

| 角色 | 浅色（默认） | 深色（备选） | 用途 |
| --- | --- | --- | --- |
| 玻璃面板底 | `rgba(255, 255, 255, 0.72)` | `rgba(18, 20, 28, 0.62)` | 所有浮层底色 |
| 玻璃描边 | `rgba(0,0,0,0.06)` 1px | `rgba(255,255,255,0.10)` 1px | 内外边界 |
| 主文 | `#171821` | `#F2F3F8` | 标题/正文 |
| 次文 | `rgba(23,24,33,0.55)` | `rgba(242,243,248,0.62)` | 辅助说明 |
| **角色暖色（品牌锚点）** | `#FFB4A2 → #FF8FAB` 线性渐变 | 同 | 气泡、情绪点亮、CTA、开关/滑块填充 |
| 情绪辅色 | `#FFD166`（喜）`#7FB7FF`（思）`#C6A8FF`（羞） | 同 | 表情驱动的微光 |
| 科技冷点 | `#6FA8FF` | 同 | 连接/Token/状态指示 |
| 语义 · 成功 | `#7FE3A1` | 同 | 状态点「可用」绿等 |
| 语义 · 警告 | `#FFB454` | 同 | |
| 语义 · 错误 | `#FF6B7A` | 同 | 破坏性操作描边、错误态 |
| 语义 · 降级 | `#C8924A` 暗琥珀 | 同 | 离线兜底/演示模式横条 |

> 主色调克制：80% 中性玻璃 + 15% 暖色（角色相关）+ 5% 冷色（系统状态）。**暖色用于品牌/CTA/开关滑块；冷色/语义色仅用于状态指示**（如 provider 状态点「可用」用成功绿，非品牌暖色）。

### 2.2 玻璃效果

**浅色主题（默认）**

```
backdrop-filter: blur(28px) saturate(180%)
background: rgba(255,255,255,0.72)
border: 1px solid rgba(0,0,0,0.06)
box-shadow:
  0 8px 32px rgba(0,0,0,0.08),
  inset 0 1px 0 rgba(255,255,255,0.55)
```

**深色主题（备选）**

```
backdrop-filter: blur(28px) saturate(180%)
background: rgba(18,20,28,0.62)
border: 1px solid rgba(255,255,255,0.10)
box-shadow:
  0 16px 48px rgba(0,0,0,0.36),
  inset 0 1px 0 rgba(255,255,255,0.08)
```

两种主题都在角色周围浮层额外叠一层径向暖色高光（10% 透明度），实现"角色辐射温度"。

> 主题切换：D4 → "界面主题" 切换 `跟随系统 / 浅色 / 深色`，默认 `跟随系统`，系统未指明时降级浅色。

> 注：以上为设计系统的**抽象数值**；上述两个 ``` 代码块是 CSS 规格（保留），**非界面线稿**。

### 2.3 字体

- 中文：**苹方 / PingFang SC** 或 **思源黑体**（Light / Regular / Medium）
- 英文：**Inter**（300/400/500/600）
- 数字与代码：**JetBrains Mono**（Token 计数、API Key、日志）
- 字号阶梯：`12 / 13 / 14 / 16 / 20 / 28 / 36`
- 行高：正文 1.5 / 标题 1.25

### 2.4 圆角与间距

- 圆角：按钮 `8` / 输入框 `10` / 卡片 `12` / 浮层 `16` / 气泡 `18`（左下/右下不对称呈"说话"感）
- 间距栅格：`4 / 8 / 12 / 16 / 24 / 32 / 48`

### 2.5 图标 & 动效

- 图标：**Lucide** 风格，1.5px 描边、圆角端点；角色相关图标允许手绘暖色点缀
- 通用过渡：`200ms cubic-bezier(0.22, 1, 0.36, 1)`
- 角色表情切换：`350–500ms` 平滑 BlendShape 过渡
- 流式文本：逐字 `30–50ms`，伴随表情实时驱动
- 微互动：按钮按压 `scale(0.97)` + 暖色辉光闪一下

### 2.6 通用组件库

#### 2.6.1 基础组件

- `GlassPanel` 三档：S 浮窗、M 浮层、L 设置窗
- `Button` 主/次/幽灵/危险 四态 × 三尺寸
- `Input / Select / Slider / Switch / Tag / Badge`
- `Avatar`（角色头像，圆形 + 情绪环）
- `Bubble`（左/右两向，含流式光标）
- `EmotionDot`（情绪标签彩点）
- `KeyCap`（热键展示，模拟物理键帽阴影）

#### 2.6.2 抽屉规范（统一三档）

| 类型 | 宽度 | 用途 | 例 |
| --- | --- | --- | --- |
| 列表型 | `280px` | 侧边导航/会话列表 | B3 会话历史、Hub 左导 |
| 表单型 | `420px` | 配置/编辑 | C 引导、确认弹窗 |
| 详情型 | `560px` | 详情/编辑器 | E2 角色详情、G2 插件详情 |

#### 2.6.3 Toast 规范

| 形态 | 位置 | 尺寸 | 持续时间 | 用途 |
| --- | --- | --- | --- | --- |
| 顶栏薄条 | Hub Window 顶部 | 全宽 × 32px | 600ms | 设置即时保存 `✓ 已保存` |
| 浮卡 | 桌面右下 | `280 × 56` | `3s` 自动消失 | 模式切换、网络恢复 |
| 长 toast | 桌面右下 | `360 × 80` | `5s` + undo 按钮 | 删除可撤销 |

堆叠规则：浮卡同时最多 3 条，新进入挤掉最早的；点击外部关闭。

### 2.7 角色 HUD 通道

> 角色本身就是状态指示器。下表定义"角色身上的 5 个 UI 槽位"，各模块向这些槽位写入信号，避免互相覆盖。

| 槽位 | 尺寸 | 用途 | 优先级 | 例 |
| --- | --- | --- | --- | --- |
| 头顶 | `12×12` 徽标 | 网络/连接相关 | P3 | J4 离线 ✈、I2 桥接消息 |
| 肩膀（右锁骨） | `14×14` 徽标 | DND / 模式 | P2 | A4 月牙 |
| 胸口 | `8×8` 暖色脉冲点 | 情绪持久态 | P4 | F4 关系等级亮起 |
| 脚下 | 4px 高色条 / 红点 | 系统/穿透/工具调用 | P1 | A3 穿透色条、G2 高风险红点 |
| 全身姿态 | 整体 BlendShape | AI 情绪/失败 | P0 | J3 歪头、欢喜跳 |

冲突规则：高优先级槽位长时间占用时压低同时段低优先级表达；最多同时显示 2 个非全身槽位。

### 2.8 破坏性操作统一规范

| 等级 | 触发示例 | 确认方式 |
| --- | --- | --- |
| ① 可恢复 | F3 删一条记忆、B3 删会话 | undo toast 5s（直接执行 + 撤销窗口） |
| ② 不可恢复但低代价 | E1 卸载角色、G1 卸载插件、**D6 高风险开关 off→on** | 二次确认弹窗，列出影响；**整张红色描边** |
| ③ 不可恢复且高代价 | D7 清空对话、清空记忆、卸载所有数据 | 二次确认 + 输入 `DELETE` 字样 |

红视觉规则：`等级②/③` 的入口按钮使用错误色描边；二次确认弹窗整张红色描边。

---

## 3. 信息架构与导航

### 3.1 三层结构

- **Layer 3 · 窗口层（Hub Window）**：一个统一窗口，左导航 + 内容区。承载设置/角色/插件/知识库。低频管理界面。
- **Layer 2 · 浮层（Floating）**：聊天浮层、引导、确认弹窗。贴附桌面或角色，半模态。按需召唤。
- **Layer 1 · 桌面层（Desktop）**：角色本体 + 桌面气泡 + 微指示。常驻、透明、可穿透。用户 90% 时间只看这层。
- 系统托盘 / 全局热键贯穿三层。

**Layer 3 不抢 Layer 1 的视觉戏份。**

### 3.2 入口关系

| 目标 | 路径 |
| --- | --- |
| 唤起聊天 | 点击角色 / `Ctrl+Shift+D` / 托盘菜单 |
| 打开 Hub | 托盘 → 设置 / 聊天浮层右上 ⚙ / 角色右键 / `Ctrl+Shift+,` |
| 切换角色（快） | 角色右键 → 切换 |
| 切换角色（慢） | Hub → 角色 → 角色库 |
| 退出/隐藏 | 托盘菜单 |

### 3.3 Hub Window 导航

**视觉真源**：`UI/03950c77-…png`（D1 Hub 骨架在该总览图中；左导航完整形态以图为准）。

- 窗口默认 `1080 × 720`，min `880 × 600`。
- 左侧导航 `280px`，可折叠为 `64px` 仅图标。导航树（组 → 叶子）：
  - 总览
  - 角色 → 角色库 / 编辑器 / 商店
  - 对话 → 历史 / 记忆 / 人格
  - 模型 API
  - 插件
  - 知识库
  - IM 桥接
  - 系统 → 通用 / 显示与窗口 / 语音 / 热键 / 隐私 / 数据 / 关于
- 顶栏 `56px`：左侧当前角色头像（hover 下拉切换），中部面包屑/全局搜索，右侧最小化/关闭。
- 状态条 `32px`：连接/内存/网络/当前模型；不显示 Token（避免与 B5/F2 冗余）。
- 实现注：无 children 的顶层组（总览/模型 API/插件/知识库）= 可点叶子；有 children 的组 = 分组标题 + 子项可点。

### 3.4 桌面层与浮层附着规则

聊天浮层默认贴在角色斜上方 16px，跟随角色拖动；靠近屏幕边缘时自动翻边。**桌面气泡（A2）和聊天浮层（B1）不同时显示**——浮层打开时桌面气泡自动收回。

---

## 4. UI 设计图索引

> **视觉真源**。19 张高保真图覆盖全部 43 屏（A–J，100%），每张图含 1–5 屏。下表为**唯一正确**的「文件 → 屏」映射（2026-06-19 由设计作者逐张核准）。**做某屏前先打开对应图**。

| 文件（`UI/`） | 覆盖的屏 | 优先级 |
| --- | --- | --- |
| `4ba6005f-0abc-45f4-9690-2c5e7af15242.png` | **A1 / A2 / A3** + 设计规范（色板/字号） | P0 |
| `8cb478c0-ff55-4b5b-8a3b-0223a13fd285.png` | **A4** 不打扰/全屏 + **J4** 离线降级 | P1 |
| `dc6e09f4-c258-4a74-aba5-ab30fa2c7ede.png` | **E1** 角色库 + **F1** System Prompt 编辑器 | P0/P1 |
| `03950c77-81a2-49b3-afaf-5e98a6c6f798.png` | **B3 / B4 / B5**（+ D1 Hub 骨架总览） | P0 |
| `d63b4f97-677b-4f72-8008-0b6834741f68.png` | **C1** 欢迎 + **C2** LLM 配置 | P0 |
| `98171885-4324-458d-9cef-c76d8794bb10.png` | **C3** 角色选择 + **C4** 首次互动 | P0 |
| `774644b7-bde9-483e-98a0-02e91c017647.png` | **D1** Hub 骨架 + **D2** 通用 | P0 |
| `36b542fb-067e-41e6-88f5-26e9ce399226.png` | **D3** 模型 API + **D4** 显示与窗口 | P0 |
| `1d7669e3-5aca-41f6-9470-2035385ca5aa.png` | **D5** 语音 + **D7** 数据管理 | P1 |
| `7075fa1f-2e1d-49e4-ad59-1881a0191a98.png` | **D6** 隐私 + **D8** 关于 | P0 |
| `60ea4a18-bf4e-4a3f-9d05-9080e49bfd8a.png` | **B1** 聊天浮层 + **B2** 流式气泡 | P0 |
| `3c9a77c6-8afc-4fc2-8fdc-b809796e8bfc.png` | **E2** 角色详情 + **E3** 角色包导入 | P1 |
| `7283fb5f-7df4-446d-b963-b7f7f95af5f8.png` | **E4** 角色编辑器 + **E5** 角色商店 | P1/P2 |
| `e53d5e72-dde2-4321-9517-7ea3a32a30b8.png` | **F2 / F3 / F4** 上下文/记忆/关系 | P1 |
| `9c5783f4-403f-46c6-8fd3-ac49fa2c9492.png` | **G1** 插件列表 + **G2** 插件详情 | P1 |
| `b837df11-8842-45c8-976d-c74fbe78f3ad.png` | **G3** 插件市场 + **G4** 开发者面板 | P1/P2 |
| `ea030568-bf0b-4f1d-8e1f-55230ec1c646.png` | **H1 / H2 / H3** 知识库/Agent工具/确认 | P2 |
| `6cbd3419-b9b6-4cb5-955d-3ac680e5c8ed.png` | **H4** 思考时间线 + **I1** AstrBot + **I2** 平台 | P2 |
| `6a38a202-bd67-4c58-a0c8-937acb9d93f7.png` | **J1 / J2 / J3 / J5** 托盘/热键/失败态/崩溃 | P0 |

> 反向速查（屏 → 文件）：A=`4ba6005f`(1-3)/`8cb478c0`(4)；B=`60ea4a18`(1-2)/`03950c77`(3-5)；C=`d63b4f97`(1-2)/`98171885`(3-4)；D=`774644b7`(1-2)/`36b542fb`(3-4)/`1d7669e3`(5,7)/`7075fa1f`(6,8)；E=`dc6e09f4`(1)/`3c9a77c6`(2-3)/`7283fb5f`(4-5)；F=`dc6e09f4`(1)/`e53d5e72`(2-4)；G=`9c5783f4`(1-2)/`b837df11`(3-4)；H=`ea030568`(1-3)/`6cbd3419`(4)；I=`6cbd3419`(1-2)；J=`6a38a202`(1-3,5)/`8cb478c0`(4)。

> 历史教训（勿重蹈）：v0.1 的文件名↔屏映射表整体错位（每张图含 2 屏、文件名被打乱），曾导致"D2/D3/D4/D8 无专属图"的错误结论。本表经作者逐张核准，是唯一权威。

---

## 5. A · 桌面层（A1–A4）

### 5.1 A1 · 桌面宠物主窗口

**视觉**：`UI/4ba6005f-….png`（A1 区）。无窗口边框、全透明背景；VRM/Live2D 渲染区默认 `320×480`（100%），可缩放 50–200%；脚下"光晕地面"可选（默认无）；hover 时全身浮现 1px 暖色描边光环。

**交互热区**（仅命中 alpha > 0.1 像素）：

| 操作 | 行为 |
| --- | --- |
| 头部点击 | `onHeadTap` → 撒娇/惊讶动作；情绪 +1 |
| 身体点击 | `onBodyTap` → 普通互动 |
| 长按 + 拖动 | 移动窗口，释放后吸附屏幕边缘 16px 内 |
| 双击 | 打开聊天浮层 B1 |
| 右键 | **桌面右键菜单**（操作行为类）：聊天/切换角色/穿透/隐藏/设置 |
| Hover > 800ms | 角色斜上 12px 浮现悬浮提示 |

- 与 E1 角色库卡片右键（资源管理类）严格区分（见 §14.2）。
- **位置记忆**：每个显示器独立记录最后位置；DPI 变化时按相对百分比重算。

### 5.2 A2 · 桌面气泡

**视觉**：`UI/4ba6005f-….png`（A2 区）。玻璃面板 + `border-left: 3px` 暖色渐变 + 指向角色头部的同色三角尾巴。

- 尺寸：max-width `280px`，min-height `40px`，padding `12 16`，圆角 `18`，左下角不对称。
- 流式输入：30–50ms 逐字，末尾 `▍` 闪烁；表情同步驱动。
- 自动消失：5s（可设 3 / 5 / 8 / 常驻）。方向自适应：上方空间不足翻转到下方。
- 交互：单击复制全文 + 短暂高亮；右键 → 在聊天浮层中查看完整对话。
- DND 态：降级为单点暖色光在角色头顶轻微脉冲。

### 5.3 A3 · 穿透 / 可交互模式视觉指示

**视觉**：`UI/4ba6005f-….png`（A3 区）。

- **切换瞬间反馈（500ms）**：可交互→穿透：青色波纹外扩 + Toast `🔇 鼠标穿透已开启 · Ctrl+Shift+P 关闭`；穿透→可交互：暖色脉冲一次 + Toast `✋ 已恢复互动`。
- **常态指示（默认关，可选）**：角色脚下 4px 高色条，可交互态暖色 30% / 穿透态青色 40% + 缓慢扫光。仅 D4 开启时显示（常态色条易与浅色壁纸冲突，默认依赖切换瞬间反馈）。

### 5.4 A4 · 不打扰 / 全屏模式徽标

**视觉**：`UI/8cb478c0-….png`（A4 区，与 J4 同图）。

| 状态 | 触发 | 视觉 | 行为 |
| --- | --- | --- | --- |
| 正常 | 默认 | 无徽标 | 全功能 |
| 不打扰 DND | 手动开 / 日历会议 | 肩膀月牙徽标 14×14 | 抑制主动消息、TTS 静音、桌面气泡降级 |
| 专注模式 | 用户启用专注 / 全屏检测 | 角色变为半透明轮廓 30% | 仅响应主动呼唤 |
| 隐藏 | 全屏游戏 / 手动 | 角色完全消失，仅托盘存在 | 后台保活 |

徽标位置绑定"右锁骨节点"骨骼，缩放变形随角色一致。

---

## 6. B · 聊天系统（B1–B5）

### 6.1 B1 · 聊天浮层

**视觉**：`UI/60ea4a18-….png`（B1 区）。

- 尺寸：默认 `420 × 560`，min `360 × 420`，max `560 × 800`；玻璃面板 + 圆角 16；分离后变独立可吸附浮窗。
- 顶栏图标动态扩展：知识库绑定追加 `📚`，桥接启用追加 `⇄`。
- 模型未连接：图标变灰 + 角色名右侧 ● 红点。
- 滚动：消息 16px 间距，连续同发言合并气泡；顶部下拉"加载更早…"；上滚后右下角浮现 `↓ N 条新消息`。

### 6.2 B2 · 流式气泡（双轨流式）

**视觉**：`UI/60ea4a18-….png`（B2 区）。

- **双轨**：文本流逐字出，遇 `<emo:xx/>` 标签立即触发 EmotionEngine；表情过渡 350–500ms 与文字**并行**。这是与 `behavior-parser.ts` 的契约。
- 特殊形态：

| 场景 | 表现 |
| --- | --- |
| 思考中（无文本） | `…` 三点呼吸光，情绪 chip = `thinking` |
| 工具调用中 | 折叠 `🔧 正在搜索网页…` 卡片，可展开看参数 |
| 错误/超时 | `border-left: #FF6B7A`，台词 `「歪头」似乎没听见，要再说一次吗？` + 重试 |
| 长文（>200 字） | 默认折叠为前 3 行 + `展开` |

- Hover 工具条：复制 · 重生成 · 在桌面气泡中重播 · 标记为记忆 · 删除。

### 6.3 B3 · 会话历史

**视觉**：`UI/03950c77-….png`（B3 区）。

- 入口：聊天浮层顶栏 → 左侧抽屉滑出（**列表型 280px**）；或 Hub → 对话 → 历史。
- 每条 64px：标题（粗）+ 末句预览（次文 12px）+ 时间 + 消息数。
- 右键：置顶 / 改名 / 导出 Markdown / 删除（§2.8 ①）。切换会话时角色播"翻书"短动画（300ms）。

### 6.4 B4 · 语音输入

**视觉**：`UI/03950c77-….png`（B4 区）。

| 状态 | 视觉 | 触发 |
| --- | --- | --- |
| 空闲 | 灰色描边话筒 | hover 高亮 |
| 录音中 | 暖色填充 + 外圈实时声波环 | 按住 / 切换 |
| 识别中 | 旋转点 + ASR 实时文字预览 | ASR 处理 |

- **长按模式**：输入框聚焦时按住空格录音。**唤醒词模式**：开启后输入区左侧浮现 `🟢 在听 · "嘿小灵"`。
- 录音时浮层底部出现 32px 高波形条；环境音过吵时浮现 `⚠ 环境噪声较高`。

### 6.5 B5 · Token 提示条（极简）

**视觉**：`UI/03950c77-….png`（B5 区）。

- 输入区底部仅显示数字 `120 t`（JetBrains Mono + 次文色）。
- **极简模式（默认开）**：只显示数字，hover 浮现详情卡：本会话累计 / 本月累计 + 费用 / 当前模型 / 预算告警。
- 接近 85% 上限：1px 进度条自动显形 + 数字变警告色；超预算 80%：输入框右下角浮现暖色徽标 `⚠ 接近预算`。
- Token 信息源 → B5（极简）/ F2（完整）/ 状态条（不显），三处去重。

---

## 7. C · 首次启动引导（C1–C4）

> 目标：90 秒内"看到角色 → 配好模型 → 听到 TA 说话 → 看到表情驱动"。
> **引导壳**（贯穿）：表单型 `480 × 600` 玻璃面板，吸附角色右侧 24px，顶部 4 步指示器 + 跳过。跳过弹确认 `跳过引导后默认角色仍可用，但你需要手动配置模型`。

### 7.1 C1 · 欢迎页

**视觉**：`UI/d63b4f97-….png`（C1 区）。

- 角色挥手 + 微笑动画；桌面气泡 A2 显示 `hi 你好~ 我是小灵，请多指教！`。
- 标题 `欢迎来到 DeskSoul`，正文交代 90 秒流程：① 配置模型 ② 选角色 ③ 说第一句话。暖色主按钮 `开始 →`；底部语言下拉。

### 7.2 C2 · LLM 配置（仅两路径 + 演示降级）

**视觉**：`UI/d63b4f97-….png`（C2 区）。

- 两路径：① 🔑 配置 API Key（OpenAI / Claude / Gemini / 通义 / DeepSeek / 自定义 OpenAI 兼容）；② 💻 本地模型 Ollama（`✓ 已检测` / `⚠ 未检测到 [安装指南]`）。
- 幽灵按钮 `暂时跳过 · 先和角色玩一下` → 进入**演示模式**（角色仅返回内置趣味台词，不连 AI）。
- API Key 展开字段：服务商 ▾ / API Key（👁 遮罩）/ 模型 ▾ / Endpoint（默认 + 自定义）/ `[测试连接]`。测试失败时角色"困惑"动作 + 气泡 `「歪头」我没法连上这个大脑诶...`。
- 底部隐私条：`🔒 你的 API Key 仅本地加密存储，不会上传任何服务器。 [了解更多]`。

### 7.3 C3 · 角色选择（默认 + 浏览库）

**视觉**：`UI/98171885-….png`（C3 区）。

- 默认伙伴「小灵」立绘大图 + 元数据（元气治愈系 · VRM 1.0 · 少女音 A）+ 双按钮 `就用 TA →` / `看看其他角色 →`。
- **"看看其他角色"闭环**：① 引导窗收为右下小气泡 `📦 引导中...` → ② Hub 跳到 E1 角色库 → ③ E1 顶部出现引导态 banner `← 选好后回到引导` → ④ 用户单击卡片"设为当前角色" → ⑤ Hub 收起、引导窗复原并进入 C4。

### 7.4 C4 · 首次互动（通用 chip）

**视觉**：`UI/98171885-….png`（C4 区）。

- 通用启动话术 chip（固定 3–5 条，不与角色绑定）：`早安！` / `给我讲个笑话` / `你叫什么名字`。
- 发送后引导窗右滑收起 → 聊天浮层 B1 在角色旁打开 → 角色流式回复 + 表情驱动。
- 完成后引导小气泡展开最终页：`✨ 准备好了！` + 快捷键提示（`Ctrl+Shift+D` 呼叫 / 拖动角色 / 右键穿透隐藏设置）+ `开始我们的故事 →` + `▢ 显示更多新手提示（首周）`。

---

## 8. D · 设置面板（D1–D8）

> 通用规则：设置项统一"卡片行"（左 Label + Description，右控件）；同主题项目一组卡片（圆角 12，内分隔线）；危险操作整张卡片错误色描边；**所有变更即时生效 + 顶栏薄条 toast `✓ 已保存`，无"保存"按钮**。开关/滑块填充用暖色品牌渐变。字段默认值见 §14.1。

### 8.1 D1 · Hub Window 骨架

**视觉**：`UI/774644b7-….png`（D1 区）+ `UI/03950c77-….png`（总览页）。骨架与导航见 §3.3。

- 总览页卡片化：当前角色、本月对话量、Token 用量、内存/CPU、最近异常、待办，每张可跳转。
- 顶栏搜索：全局模糊匹配，命中后定位并高亮 800ms；中英混合可达。

### 8.2 D2 · 通用

**视觉**：`UI/774644b7-….png`（D2 区）。

字段 → prefs（分组）：
- **启动**：开机自启动 `general.launchAtLogin`、启动时显示 `general.startupShow`（角色+托盘/仅托盘/不自动显示）。
- **语言与地区**：语言 `general.language`、时区 `general.timezone`、24 小时制 `general.hour24`。
- **更新**：自动检查更新 `general.autoUpdate`、当前版本 + `[检查]`、通道 `general.updateChannel`（稳定版/预览版）。
- **通知**：桌面通知 `general.desktopNotifications`、角色主动发言 `general.proactiveSpeech`（默认关）→ 开后缩进子项：主动发言频率 `general.proactiveFreq`（滑块 低↔高）、不打扰时段 `general.dndStart/dndEnd`。
- **Agent 思考过程**：显示模式 `general.agentThinkingDisplay`（完整/仅工具/隐藏）。
- **开发者**：开发者模式 `general.developerMode`（开后插件页出现「开发者面板」入口）。

### 8.3 D3 · 模型 API（双栏）

**视觉**：`UI/36b542fb-….png`（D3 区，双栏）。

- **左栏 Providers** + 状态点：绿（`--ds-success`）= 已配置可用 / 灰 = 待填 Key / 红（`--ds-danger`）= 测试失败；底部 `+ 添加 Provider`。
- **右栏详情**：provider 名 · 格式 / API Key（👁 遮罩，点眼睛显示 5s 后自动遮回）/ Endpoint / 默认模型 ▾ / 可用模型勾选 / 高级（超时/重试/代理/Stream 协议）/ `[测试连接]` `[删除该 Provider]`。
- **页底 · 预算告警卡**：启用预算告警 `budget.enabled` / 本月上限 `budget.monthlyCap` / 已使用（计量占位）/ 告警阈值 `budget.warnAt`（滑块）/ 达上限时 `budget.onExceed`（提醒/提醒并暂停）。
- **页底 · 离线兜底卡**（J4 联动）：`offline.fallbackMode`（切 Ollama / 演示模式 / 报错）+ Ollama 备用模型 `offline.ollamaModel`。
- 行为契约：选 provider → `app.prefs.set model.activeProvider`；选模型 → `model.activeModel`；Key → `provider.saveKey`（Keychain）；测试 → `provider.testConnection`。chat.send 动态解析当前 provider/model（worker 零改动，缺省走 dialect 默认模型）。
- 复用提示：provider-config 积木（KeyInput / ProviderList / 状态点）与 C2 引导共用。

### 8.4 D4 · 显示与窗口

**视觉**：`UI/36b542fb-….png`（D4 区）。

字段 → prefs（分组）：
- **角色**：缩放 `display.characterScale`（滑块 50–200%，实时预览，拖动即时缩放、松手持久）、始终置顶 `display.alwaysOnTop`、鼠标穿透 `display.clickThrough` + 切换热键 + 穿透常态色条 `display.clickThroughBar`、LookAt `display.lookAt` + 强度 `display.lookAtStrength`、物理摆动 `display.physics`、脚下光晕 `display.footGlow`、界面主题 `display.theme`（跟随系统/浅色/深色）。
- **多显示器**：跟随显示器 `display.followDisplay`、跨屏拖动 `display.crossScreenDrag`（吸附/自由）、自动适配分辨率。
- **不打扰**：全屏自动隐藏 `display.fullscreenHide`、游戏检测 `display.gameDetect`、视频会议降级 `display.meetingDowngrade`、计划不打扰时段。
- **⚠ 实验性**（警告色描边）：桌面壁纸层模式 `display.wallpaperMode`（+ 穿透冲突警告语）。

### 8.5 D5 · 语音

**视觉**：`UI/1d7669e3-….png`（D5 区）。

- **输入 ASR**：启用 / 引擎（Whisper.cpp 本地）/ 模型（small.zh 等，含下载进度）/ 语言 / 麦克风 + 电平 + 测试录音 / 唤醒词（默认关）+ 词条 / 按住说话 Space / 静音阈值。
- **输出 TTS**：启用 / 引擎（Edge TTS）/ 默认音色 + 试听 / 语速 / 音调 / 角色独立音色。
- **嘴型同步**：嘴型驱动 / 模式（音量包络/音素分析）/ BlendShape 映射 / 强度。
- **高级**：用户开口打断 TTS / 角色说话时降低系统音量。
- 模型未下载时按钮显示圆形进度环；TTS 试听时角色真实做嘴型。

### 8.6 D6 · 隐私

**视觉**：`UI/7075fa1f-….png`（D6 区）。

字段 → prefs（分组）：
- **数据去向**：区域选择（本地优先不出境）+ 说明（文本/记忆/语音本地，截屏/摄像头默认禁用需授权）。
- **API Key 加密**：加密方式（AES-256 + 系统密钥环）、主密码 `privacy.masterPassword`（默认关）。
- **内容上送**：对话文本上送 `privacy.contentUpload`、对话脱敏 `privacy.masking`、上下文窗 `privacy.contextWindow`、长期记忆 `privacy.longTermMemory`。
- **系统访问（默认全关）**：剪贴板 `privacy.clipboard`、屏幕内容截屏 `privacy.screenshot`、摄像头 `privacy.camera`、文件系统、系统通知 `privacy.systemNotify`、麦克风 `privacy.microphone`、情感画像 `privacy.affectionProfile`。
- **遥测与崩溃**：匿名统计 `privacy.anonymousStats`、崩溃上报 `privacy.crashReport`、本地日志保留 `privacy.logRetentionDays`。
- **行为契约（§2.8 ②级）**：高风险开关（**截屏 / 摄像头 / 文件系统**）`off→on` 必弹**整张红描边**二次确认，确认才落盘、取消回退；关闭与非高风险（麦克风等）直通。

### 8.7 D7 · 数据管理（仅手动）

**视觉**：`UI/1d7669e3-….png`（D7 区）。

- **备份与导出**：`[导出 .dsbak]`（含角色/会话/记忆/配置，**不含 API Key**）。
- **导入**：拖入 `.dsbak / .dspack` 或点选；导入冲突策略。
- **存储占用**：分类条形（角色与模型/对话历史/长期记忆/语音模型/日志）+ `[打开数据目录]`。
- **⚠ 危险区**（红描边，按 §2.8 三档）：清空对话历史 `[清空]`（②）、清空长期记忆 `[清空]`（②）、重置全部设置（保留角色）、卸载所有数据 `[卸载]`（③，输 `DELETE`）。

### 8.8 D8 · 关于

**视觉**：`UI/7075fa1f-….png`（D8 区）。

- **头部卡**：Logo + DeskSoul + 版本 `v0.1.0 · Beta` + 构建日期 + slogan + `[检查更新][打开官网][GitHub][社区]`。
- **致谢卡**：开源项目列表 + `[完整开源许可证]`。
- **反馈与帮助卡**：用户手册 / 报告问题(GitHub Issues) / 社区(Discord) / 给作者写信。
- **诊断卡**：`[生成 .dsdiag]`（脱敏日志 + 系统信息 + 配置摘要，剔除 Key/对话）。
- **法律卡**：服务条款 / 隐私政策 / 开源许可证(MIT)。
- 行为契约：所有外链经 `app.openExternal{url}`（仅放行 http/https）。检查更新（无 updater）、诊断包生成（无聚合后端）= MVP 存而不接（disabled）；七连击开发者模式为彩蛋（后续）。

---

## 9. E · 角色管理（E1–E5）

### 9.1 E1 · 角色库

**视觉**：`UI/dc6e09f4-….png`（E1 区，与 F1 同图）。

- 卡片 220×320（立绘 200×200 + 元数据 120px）；当前激活：左上 ● 暖色实心 + 整张暖色描边；配置异常：右上 ⚠ + tooltip。
- 交互：hover 上浮 4px + idle 微动；单击 → 右抽屉详情 E2；双击 → 热切换为当前角色。
- 右键（**库卡片资源管理类**，与 A1 桌面右键区分）：设为当前 / 编辑 / 复制 / 导出 .dspack / 在文件夹中显示 / 卸载（§2.8 ②）。
- 引导期：顶部出现引导态 banner `← 选好后回到引导`。

### 9.2 E2 · 角色详情（详情型抽屉 560px）

**视觉**：`UI/3c9a77c6-….png`（E2 区）。

- 立绘 320×400（VRM 可拖动旋转）+ 即时预览 chip `[Idle][Wave][Sleep]`。
- 主操作：`[设为当前角色] [编辑] ⋮`；⋮ = 复制 / 导出 / 重命名 / 重置情绪映射 / 卸载（红）。
- 信息区：元数据（作者/版本/许可证/大小/安装时间）、绑定（默认模型/音色/System Prompt/动画集/情绪映射）、标签/描述。

### 9.3 E3 · 角色包导入

**视觉**：`UI/3c9a77c6-….png`（E3 区）。

- 入口三处：库顶栏「导入」/ 拖文件到 Hub / 拖到桌面角色身上。拖入时全屏暖色 30% 覆盖引导 `📦 松手即可导入 .dspack / 角色文件夹`。
- 释放后弹**导入确认对话框（表单型 420px）**：角色名+版本 / 缩略 + 作者 + 许可证 + 类型 + 大小 / 权限请求（来自 manifest，可后改）/ `⚠ 未签名` 提示 / `[取消][导入]`。
- 进度态：按钮变环形进度（解压→校验→注册）；完成 toast `✓ 已加入角色库 · [查看]`；错误态 + `[查看完整错误]`。

### 9.4 E4 · 角色编辑器

**视觉**：`UI/7283fb5f-….png`（E4 区）。

- 四 Tab：外观 / 人格 / 动画 & 情绪 / 高级。左实时预览（立绘 + 表情驱动 + `[试讲台词]`），右编辑面板。
- **试讲**：按下用当前 Prompt 生成 30 字内问候并播表情。
- 动画 & 情绪 Tab：动画列表 + 8 基础情绪映射表（情绪→动画+时长），可加自定义。
- 高级 Tab：Hooks（`on_idle/on_click_head/on_message`）、权限声明、版本号、导出 `.dspack`。

### 9.5 E5 · 角色商店（V2，模板复用）

**视觉**：`UI/7283fb5f-….png`（E5 区）。

- 结构与 G3 共用 `MarketPageTemplate`（§14.4），仅换数据源：搜索/分类/排序/许可证 + 特色推荐横幅 + 热门卡片 + 标签 chip。
- 详情页：大图 + 截图轮播（含运行时录屏）+ 评分 + 评论 + 版本历史 + 许可证 + 举报。
- NSFW 入口需 D6 隐私页同步开关后显示。离线降级：`角色商店需要联网 · [手动导入 .dspack]`。

---

## 10. F · 记忆与人格（F1–F4）

### 10.1 F1 · System Prompt 编辑器

**视觉**：`UI/dc6e09f4-….png`（F1 区，与 E1 同图）。

- 入口：Hub → 对话 → 人格；或 E4 "人格 Tab"。
- 变量：`{{user_name}} {{date}} {{time}} {{recent_topic}} {{user_interests}}`；内置 4 模板（治愈伙伴/工作助理/学习伴侣/自由发挥）。
- 诊断条：实时检查（输出协议片段 / 字数 / 自相矛盾）；编辑历史最近 10 版可回滚 + 双栏 diff；`[小测一句]` 用当前 Prompt 生成 30 字问候 + 实时表情。

### 10.2 F2 · 短期会话上下文

**视觉**：`UI/e53d5e72-….png`（F2 区）。

- 入口：聊天浮层顶栏「⊙ 记忆」（**列表型抽屉 280px**）。
- 显示：会话名 + 消息数 / 发送给模型时使用最近 N 轮 / 当前已用 tokens + 进度条 / 消息列表。
- 每条 `👁` 隐藏（不发模型）/ `📌` 固定（永远在上下文顶部）；操作：清空当前会话 / 开新会话 / 固定本条。

### 10.3 F3 · 长期记忆事实清单

**视觉**：`UI/e53d5e72-….png`（F3 区）。

- 入口：Hub → 对话 → 记忆。自动归类（人物/工作/兴趣/习惯/场景，由 LLM 调用做）。
- 每条：事实 + 来源（可点跳回原会话）+ 相关性 5 级（最近召回频次）+ `📌 ✏ 🗑`；钉住条目永远参与 RAG。
- 删除：撤销 toast 5s（§2.8 ①）。`+ 手动添加`。仅本地存储，sqlite-vec 索引。

### 10.4 F4 · 关系 / 熟悉度面板

**视觉**：`UI/e53d5e72-….png`（F4 区）。

- 入口：Hub → 对话 → 人格 → 关系；或 E2 "关系" chip。
- 熟悉度 Lv（基于交互次数+时长+记忆条数）+ 进度；里程碑（内置 ~15 个，解锁时 A2 弹贺）；当前印象（记忆 TopN 摘要句，每周更新）+ `[让 TA 重新认识你][查看影响因素]`；情绪基调（基于用户**自述**，不做语音/表情推断）。
- 整面板可在 D6 隐私 → "情感画像" 整体关闭。

---

## 11. G · 插件（G1–G4）

### 11.1 G1 · 已安装插件列表

**视觉**：`UI/9c5783f4-….png`（G1 区）。

- 顶栏：导入文件 / 浏览市场 / 🛠 开发者面板 / 搜索 / 类型 · 状态 · 排序筛选。
- 每行：类型图标（🔧工具暖/🎮玩法紫/⚡Provider 青/👤角色粉）+ 名称版本 + 提供方 + 描述 + 权限标 + 调用统计 + 启用开关。警告条（依赖/冲突/未签名/崩过）= 黄横幅。
- 单击行 → 右抽屉详情 G2；右键：重载 / 删除（§2.8 ②）/ 在文件夹中显示 / 报告问题。
- 类型筛选 chip：全部 / 工具 / 玩法 / Provider / 角色 / 主题 / 语音。

### 11.2 G2 · 插件详情 / 权限授权（详情型抽屉 560px）

**视觉**：`UI/9c5783f4-….png`（G2 区）。

- 头部：图标 + 名称 + 版本 + 提供方 + 签名标 + `[启用][立即调用][更新] ⋮`。
- 区块：描述 / 配置 / 权限 / 工具暴露 / 钩子 / 元数据（作者/仓库/许可证/安装时间/本地路径 `[打开]`）。
- **首次启用权限确认**：列出申请权限（含具体域名）+ `[全部拒绝][仅本次][允许]`。
- **高风险权限**（文件读写/麦克风/摄像头/截屏）：Switch 红色态；弹窗须勾 `▢ 我理解风险` 才可"允许"；授予后角色脚下短暂 ⚠ 红点 1s（§2.7）。

### 11.3 G3 · 插件市场（V2，模板复用）

**视觉**：`UI/b837df11-….png`（G3 区）。与 E5 共用 `MarketPageTemplate`（§14.4）。详情页含权限预览（**装前看权限**）。离线降级：`[手动导入 .dsplugin / 文件夹]`。

### 11.4 G4 · 开发者面板

**视觉**：`UI/b837df11-….png`（G4 区）。

- 入口：Hub → 插件 → 🛠 开发者面板（默认隐藏，需 D2 开发者模式 或 D8 七连击）。
- 区块：Console（log/info/warn/err + 级别筛选 + 仅当前插件 + 清空/导出，错误堆栈可跳转/生成 Issue）、沙箱状态、性能（CPU/内存/调用/平均耗时）、工具调用回放（复用历史参数重放）、manifest.json（JSON Schema 实时校验 + 红波浪线）。
- 重载 `Cmd+R` 热加载不重启；暂停沙箱便于断点；外部 dev server `🟢 已连接 ws://localhost:9999` 保存自动重载。

---

## 12. H · 知识库 & Agent（H1–H4）

### 12.1 H1 · 知识库列表 + 文件导入

**视觉**：`UI/ea030568-….png`（H1 区）。

- 列表：每个知识库卡（文件数/段数/大小、最后更新、绑定角色、`[打开][⋮]`）；索引中显示进度条 + 剩余时间 + `[暂停]`。
- 详情页 Tab：**设置**（绑定角色 / 分块策略 / 分块大小+重叠 / Embedding 模型 + 测试 / 召回 TopK / 相似度阈值 / 自动注入 + 位置）、**文件**（全部/待索引/失败/剪藏，每行类型+段数+更新+⋯，失败行展开原因 + 重试/删）、**检索测试**（输入 query → 命中条目 + 相似度 + `[打开原文][模拟注入]`）、**分块预览**（左原文右分块对照）。
- 性能指示常驻顶部：`● 空闲 · 队列 0` / `● 索引中 N/M`。

### 12.2 H2 · Agent 工具启用

**视觉**：`UI/ea030568-….png`（H2 区）。

- 内置工具（网页搜索/剪贴板/文件读取/文件写入/日历/定时器/天气）+ 来自插件的工具，各带启用开关。
- 风险三档（低绿/中黄/高红）；"调用前确认"：从不 / 每次 / 仅高风险；**高风险强制"每次确认"且不可改（🔒）**。
- 关闭某工具 → System Prompt 自动剔除该 tool 描述。

### 12.3 H3 · 高风险工具确认弹窗（表单型 420px）

**视觉**：`UI/ea030568-….png`（H3 区）。

- 必须明确目标 + 预览（不省略路径/URL/命令）；如文件写入显示目标路径 + 内容预览（可滚动）+ 大小。
- `▢ 该会话内不再询问相同操作`（仅限本会话 + 相同工具 + 相同根路径）；`[拒绝][允许]`。
- 拒绝后角色"挠头" + 气泡 `好的，我没动那个文件`；30s 不响应自动拒绝。

### 12.4 H4 · Agent 思考过程时间线

**视觉**：`UI/6cbd3419-….png`（H4 区，与 I1/I2 同图）。

- 角色气泡内时间线：思考步（暖色虚线轨道）+ 工具步（青色实线轨道，>2s 标黄），工具结果可展开看 JSON/Markdown。
- 思考步骤文字柔和处理（次文 + italic + 0.85），与最终回复区分；**不展示 system prompt 内容**。
- 显示模式由 D2 控制（完整/仅工具/隐藏）；折叠后只显示 `▶ N 步 · 共 X s` + 最终回复；底部 `共 ~Xs · N 步 · ¥费用 · [复制全部][重放]`（重放复用历史工具结果，不重发）。

---

## 13. I · IM 桥接（I1–I2）

### 13.1 I1 · AstrBot 桥接配置

**视觉**：`UI/6cbd3419-….png`（I1 区）。

- **未配置态**：说明 + `[一键安装 AstrBot][我已有 AstrBot]` + `[3 分钟视频说明]`。
  - 一键安装闭环：确认下载（~85MB 到 `~/.desksoul/astrbot/`）→ 进度环（下载→解压→启动子进程）→ 自动填 `本地启动 ws://localhost:7860` → toast `✓ AstrBot 已就绪`。
- **已配置态**：模式（远程 URL / 本地启动）+ Endpoint + Token + 心跳间隔 + 状态（延迟）+ 版本 + `[测试连接][断开][保存]`。本地启动显示 `子进程 · pid · [查看日志][重启]`。
- **代表角色映射**：● 角色 → 模型，`+ 添加映射`。
- **同步范围**：共享 Persona（默认开）/ 共享长期记忆（默认关 + 警告）/ 共享会话历史（默认关）/ IM 消息回桌面提示（默认开）。

### 13.2 I2 · 平台连接状态

**视觉**：`UI/6cbd3419-….png`（I2 区）。

- 平台卡（QQ/Discord/Telegram…）：状态色（`● 在线` 暖色脉冲 / `● 离线` 灰 / `⚠ 鉴权失败` 红 + `[修复]` / `🔄 连接中` 旋转）+ Bot 标识 + 24h 消息量 + `[配置]`；`+ 添加平台`。
- 平台配置抽屉（详情型 560px）：连接方式 / Endpoint / Token / 能力开关 / 权限白名单（由 AstrBot 后端完成，DeskSoul 仅透传）。
- 桌面联动：桌面气泡偶现 IM 通知（头顶槽位 §2.7）；B1 顶栏多 ⇄ 图标；B3 桥接来源会话用淡青边条 + 平台 chip 区分。

---

## 14. J · 系统集成（J1–J5）

### 14.1 J1 · 系统托盘菜单

**视觉**：`UI/6a38a202-….png`（J1 区）。

- 菜单项：标题（DeskSoul + 版本 + 连接态）/ 跟小灵聊聊 `Ctrl+⇧D` / 显示隐藏角色 `Ctrl+⇧H` / 鼠标穿透 `Ctrl+⇧P` / 不打扰 `Ctrl+⇧M` / 当前角色子菜单 + 角色库 / 打开 Hub / 设置 `Ctrl+⇧,` / 反馈 / 退出。
- 跟随系统原生菜单（Win 11 acrylic / Mac vibrancy）；托盘图标三态（默认头像简笔 / 思考呼吸 / 异常红点）；hover 800ms tooltip。
- 左键单击=切换显示隐藏（最高频）/ 左键双击=打开聊天 / 右键=菜单 / 中键=切穿透。

### 14.2 J2 · 全局热键录制器（表单型 420px）

**视觉**：`UI/6a38a202-….png`（J2 区）。

- 录制：点击按键区 → 监听（暖色描边脉冲）→ 按下组合即捕获（KeyCap 组件，按下 1px 内陷动画）。
- 冲突检测：命中已知系统/应用快捷键 → 黄警告 `已被「X」占用` + `[仍要使用][换一个]`。
- 限制：不允许单键 / 纯修饰键 / ESC；多平台自动转换（Mac Ctrl→⌘ / Alt→⌥）。
- 热键总览（D2 → 热键页）：表格列出所有可绑定功能 + 当前键 + 录制 + 一键全恢复。

### 14.3 J3 · API 失败的"歪头疑惑"角色态

**视觉**：`UI/6a38a202-….png`（J3 区）。

- 桌面层：错误色左条气泡 + `[重试][换个模型]` + 全身姿态歪头 350ms（情绪 confused）。聊天浮层错误气泡：`border-left: #FF6B7A` + 原因 + `[重试本条][换个模型 ▾][复制错误]`。
- **错误分级文案**（绝不直抛 `Error: 401`，详情仅在二级"复制错误"）：

| 类型 | 角色台词 | 操作 |
| --- | --- | --- |
| 网络超时 | "「歪头」我没法连上大脑诶…" | 重试 / 换模型 |
| 401 / Key 错误 | "「眨眼」哎，钥匙好像不对" | 改 Key |
| 配额耗尽 | "「叹气」今天的额度用完啦" | 切模型 / 加预算 |
| 内容审查 | "「捂嘴」这个话题我们换一个？" | 换话题 |
| 模型异常 | "「困惑」大脑卡了一下，再说一次？" | 重试 |
| 流式中断 | "「咳」啊…刚才说到哪了？" | 继续 / 重生成 |

### 14.4 J4 · 离线降级提示

**视觉**：`UI/8cb478c0-….png`（J4 区，与 A4 同图）。

- 桌面层：角色头顶槽位（§2.7）极小 ✈ 飞机模式徽标（12×12 淡青半透明）。
- 聊天浮层顶栏：1px 横条 `⚠ 离线模式 · 当前回复来自本地兜底`（降级色 `#C8924A`）。
- 降级阶梯（D3 离线兜底选）：切 Ollama / 演示模式 / 报错。
- 网络恢复：右下 toast `🟢 网络已恢复 · 已切回 X`，飞机徽标 fade，角色"舒展" idle。

### 14.5 J5 · 崩溃上报对话框（表单型 420px）

**视觉**：`UI/6a38a202-….png`（J5 区）。

- 内容：友好文案 + 将上送内容（脱敏后：错误堆栈 / 系统信息 / 配置摘要不含 Key/对话 / 最近 200 行日志）+ `[查看完整内容]`（与上送字节一致）。
- 选项：`▢ 附加我的描述（可选）` / `▢ 以后自动上报（默认不勾）`；`[不上报][仅这次][上报]`。
- 上送后 toast `已收到，谢谢 · ID: xxxx`；失败本地排队下次重试，连续 3 次失败提示手动上送。

---

## 15. 附录

### 15.1 关键开关默认值表

> **prefs 默认真源**（与 `packages/protocol/src/prefs.ts` 的 `PrefsSchema` 默认值一致）。

| 开关 | 位置 | 默认 | 备注 |
| --- | --- | --- | --- |
| 开机自启动 | D2 | 开 | 桌宠常驻语义 |
| 角色主动发言 | D2 | 关 | 主动行为不打扰原则 |
| Agent 思考过程显示 | D2 | 完整 | 可教育用户 |
| 开发者模式 | D2 | 关 | 普通用户隐藏 |
| 始终置顶 | D4 | 开 | 桌宠核心体验 |
| 鼠标穿透 | D4 | 关（可交互） | 默认能点 |
| LookAt 看向鼠标 | D4 | 开 | 增强活感 |
| 角色脚下光晕 | D4 | 关 | 默认通透不抢戏 |
| A3 常态色条 | D4 | 关 | 切换瞬间反馈足够，常态易冲突 |
| 界面主题 | D4 | 跟随系统（未指明降级浅色） | 浅色为产品默认气质 |
| 实验性壁纸层模式 | D4 | 关 | 实验性必关 |
| 唤醒词 | D5 | 关 | 隐私优先 |
| 主密码加密 | D6 | 关 | 高门槛功能 |
| 长期记忆 | D6 | 开 | 角色感关键 |
| 剪贴板/截屏/摄像头/文件读写 | D6 | 全关 | 隐私优先（高风险 off→on 需 §2.8 ②确认） |
| 麦克风 | D6 | 开 | TTS/ASR 默认能用 |
| 情感画像（F4 关系面板） | D6 | 开 | 可关闭 |
| 匿名使用统计 | D6 | 关 | 默认不统计 |
| 崩溃自动上报 | D6 | 开 | 但首次询问 |
| 启用预算告警 | D3 | 关 | 用户主动开 |
| 共享 Persona（桥接） | I1 | 开 | 桥接核心目的 |
| 共享长期记忆（桥接） | I1 | 关 | 防私域泄漏到群聊 |
| 共享会话历史（桥接） | I1 | 关 | 隔离工作/私聊 |
| IM 消息回桌面提示 | I1 | 开 | 单向通知不耗资源 |

### 15.2 已知约束与未来迭代

1. **B1 顶栏图标动态扩展**：桥接启用追加 `⇄`，知识库绑定追加 `📚`。
2. **关键开关默认值集中维护**：以 §15.1 为准（同步 `PrefsSchema`）。
3. **角色右键菜单上下文区分**：A1 桌面角色右键 = 操作行为类；E1 库卡片右键 = 资源管理类。两套共用一个 `ContextMenu` 组件，按上下文传不同 items。

### 15.3 设计交付状态

**已 100% 出图**：A–J 共 43 屏全部有高保真图覆盖（19 张 PNG），映射见 §4。无"待出图"项。

### 15.4 复用模板说明

**MarketPageTemplate**（E5 角色商店 + G3 插件市场共用）：顶栏（搜索 + 分类 + 排序 + 筛选 + 用户区）/ 特色推荐横幅 / 分类入口 / 热门趋势新上线滚动卡片 / 标签 chip 流 / 详情页（大图 + 截图轮播 + README + 评分 + 评论 + 版本历史 + 许可证 + 举报）/ 离线降级手动导入。

详情中"安装"按钮三态：默认 `[安装]` / 已装 `[已装✓]` / 升级 `[更新到 vX]`。

---

*— v0.2（2026-06-19）重写：视觉真源为 `UI/` 高保真图，本文档仅承载图索引 + 实现契约；已删除 v0.1 的全部 ASCII 线稿（避免与高保真图冲突误导）。*
