# better-sqlite3 预编译二进制走 npmmirror 镜像（本机直连 GitHub releases 不通；见 memory: 项目环境网络约束）。
# 仅镜像二进制下载，不覆盖 npm registry——CI(windows-latest) 走默认源不受影响。
better_sqlite3_binary_host_mirror=https://registry.npmmirror.com/-/binary/better-sqlite3
