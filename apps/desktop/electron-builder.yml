appId: app.openpet.desktop
productName: openpet
directories:
  output: release
files:
  - out/**/*
extraResources:
  - from: characters
    to: characters
  - from: resources/tray
    to: tray
win:
  target: nsis
  icon: build/icon.ico
  artifactName: ${productName}-${version}-${arch}.${ext}
