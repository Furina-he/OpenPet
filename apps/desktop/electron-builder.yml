appId: app.desksoul.desktop
productName: DeskSoul
directories:
  output: release
files:
  - out/**/*
extraResources:
  - from: characters
    to: characters
win:
  target: nsis
  artifactName: ${productName}-${version}-${arch}.${ext}
